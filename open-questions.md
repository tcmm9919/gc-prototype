# Open questions / risks

Открытые вопросы по продукту, требующие решения банка/методолога.
Источник: UX-аудит таба «Скоринг» (июнь 2026).

## Скоринг (аудит таба, 2026-06-07)

1. **Формула итогового балла не подтверждена банком.** В прототипе реализована
   гипотеза «взвешенное среднее» (`weighted-average-v1`,
   `lib/scoring/formula.ts`). Если реальный API отдаёт балл, который не
   сходится с формулой (>5 пунктов) — UI показывает warning-icon у балла.
   Нужна реальная формула от методолога → переделать
   `<ScoreFormulaBreakdown>`.

2. **Источник весов.** Сейчас веса (35/40/15/10) живут в конфиге
   `risk_v2.4` (`lib/scoring/sources.ts` → `RISK_WEIGHTS`). Подтвердить:
   редактируются ли они методологом в Настройки → Риск-факторы, и нужна ли
   версионность конфига с историей изменений.

3. **Пороги confidence.** Подсветка «уверенности» захардкожена:
   <70% — янтарный, <50% — красный, рекомендация ручной проверки.
   Подтвердить пороги и требуемое действие оператора при низком confidence
   (JTBD M3.5).

4. **Размещение и роль ПДЛ-проверки.** ~~ПДЛ — не подскор, вне формулы.~~
   **РЕШЕНО (2026-06-09):** по запросу дизайнера ПДЛ включён в формулу как
   критический источник (вес 10%, конфиг `risk_v2.5`), приглушённость убрана,
   плашка — красная. Открытый вопрос к банку: корректно ли, что совпадение по
   ПДЛ — это весовой вклад во взвешенное среднее (сейчас так), или это должен
   быть жёсткий override (любое совпадение → критический риск независимо от
   остальных источников)? Сейчас — весовой вклад.

5. **Поведение при расхождении API-балла и формулы.** Реализован warning +
   tooltip «Сообщить методологу». Уточнить желаемый процесс: куда уходит
   сигнал (письмо? тикет? аудит-событие?) и каков допустимый порог
   расхождения (сейчас |Δ| > 5).

6. **Глубина и стоимость истории/пересчёта.** История изменений риска
   сейчас — mock на 30 дней (9–11 расчётов). Подтвердить: глубина хранения,
   откуда берётся история (audit-события?), и реальную стоимость/латентность
   пересчёта агентов (сейчас mock ~7.5 сек/агент, ~$0.20 за прогон).
